import Labs.Utility.FileStream;

public class Main {
    public static void main(String[] args) {

        //FileStream fileStream = new FileStream();

        //int[][] two = new FileStream().readArray("C:\\Users\\Yrulewet\\IdeaProjects\\TasksJava\\Input\\Input.txt")      // двумерный
        //        .stream().map(e -> e.stream().mapToInt(Integer::intValue).toArray()).toArray(int[][]::new);

        //Integer[] one = new FileStream().readArray("C:\\Users\\Yrulewet\\IdeaProjects\\TasksJava\\Input\\Input.txt").get(0).toArray(new Integer[0]); // одномерный

    }
}